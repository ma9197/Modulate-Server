name = "modulateserver"
main = "src/index.ts"
compatibility_date = "2024-01-01"

[vars]
# Public environment variables (non-sensitive only)
# DO NOT PUT SECRETS HERE - use Cloudflare dashboard or wrangler secret put

# Local development settings
[env.dev]
# For local testing with wrangler dev, secrets go in .dev.vars file (gitignored)

